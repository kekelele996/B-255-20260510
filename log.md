# 项目开发轨迹与模型逻辑日志

本日志记录了“三维虚拟工厂”项目的迭代过程、模型动画逻辑及视觉优化轨迹。

## 📅 开发日志记录

### Phase 1: 核心三维场景搭建
- **任务**: 建立基于 R3F 的 Canvas 环境。
- **轨迹**: 
    - 引入 `PerspectiveCamera` 设置初始工业视角。
    - 部署 `FactoryLayout` 组件，使用无限网格（Infinite Grid）定义坐标系。
    - 实现地面反射与 `ContactShadows`（接触阴影）以增强模型落地感。

### Phase 2: 工业设备动态化
- **RoboticArm 逻辑**: 采用正向动力学模拟，使用 `Math.sin` 和 `Math.cos` 驱动基座旋转与多级关节联动。
- **Machine 逻辑**: 
    - 压力机（Press）：垂直往复运动轨迹。
    - CNC 中心：水平双轴循环切削动作。
    - **更新**: 为面板添加了频率为 5Hz 的动态扫描条动画。

### Phase 3: 环境与粒子系统集成
- **EnvironmentManager**: 实现了 `Sky` 和 `Stars` 的状态切换逻辑。当时间设为 `NIGHT` 时，降低 `ambientLight` 强度并激活车间内部的蓝/橙点光源。
- **WeatherManager**: 
    - 雨（Rain）：高垂直速度粒子，淡蓝色材质。
    - 雪（Snow）：带有 X 轴偏移（Drift）的低速粒子，纯白材质。

### Phase 4: 相机轨迹巡航系统
- **算法**: 使用 `Vector3.lerp`（线性插值）实现相机位置的平滑过渡。
- **路径点（Waypoints）**:
    1. `[20, 15, 20]` - 全景概览。
    2. `[-20, 10, 10]` - A 侧生产线近景。
    3. `[0, 8, -20]` - 后方质检区视角。
    4. `[15, 5, 5]` - 机械臂细节观察位。

### Phase 5: UI 与 本地化优化
- **轨迹**: 
    - 移除所有英文标签，全局替换为中文。
    - 删除了实验性的“AI 洞察”功能，简化控制流程。
    - 移除了空白装饰性模型（如浮动无人机），专注于生产线核心逻辑。

## ⚙️ 模型运行状态说明

| 设备名称 | 动作轨迹类型 | 循环周期 (s) | 状态 |
| :--- | :--- | :--- | :--- |
| 机械臂 A1 | 周期性抓取旋转 | ~4.2s | 运行中 |
| 压力机 P1 | 垂直冲压 | ~2.1s | 运行中 |
| CNC 加工中心 | XY 轴圆周运动 | ~3.1s | 运行中 |
| 导航系统 | 线性路径插值 | 连续 | 待命 |

---
*日志结束*
